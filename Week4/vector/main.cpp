#include <iostream>
#include "vector.hpp"

using namespace std;

int main()
{
    /*
    Vector3D a = {5, 0, 0};
    Vector3D b = {0, 5, 0};
    // char path[] = "serialized.bin";
    // serialize(a, path);
    // Vector3D b = deserialize(path);
    // cout << length(a) << endl;
    // cout << length(b) << endl;
    // cout << scalarProduct(a, b) << endl;
    // cout << arePerpendicular(a, b) << endl;
    
    Vector3D c = vectorProduct(a, b);
    cout << c.x << ' ' << c.y << ' ' << c.z << endl;
    cout << length(c);
    */
    return 0;
}
